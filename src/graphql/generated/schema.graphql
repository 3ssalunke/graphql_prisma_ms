type Query {
  books: [Book]
}

type Mutation {
  createBook(book: BookInput!): Book!
}

type Book {
  id: ID!
  title: String!
  author: String!
}

type BookInput {
  title: String!
  author: String!
}
